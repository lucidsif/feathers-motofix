const id = (motorcycle) => motorcycle.id
const year = (motorcycle) => motorcycle.year
const make = (motorcycle) => motorcycle.make
const model = (motorcycle) => motorcycle.model
const submodel = (motorcycle) => motorcycle.submodel
const detail = (motorcycle) => motorcycle.detail
const vehicletype = (motorcycle) => motorcycle.vehicletype

export default {
  Motorcycle: {
    id,
    year,
    make,
    model,
    submodel,
    detail,
    vehicletype,
  }
}
